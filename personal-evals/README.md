# Personal evals

Open scenarios for `ruby-skill-bench`. Each folder targets a skill that lives in this repo.

Moved here from `rails-agent-skills` because the skills themselves moved:

| Scenario | Target skill |
|----------|----------------|
| `skill-ddd-rails-modeling` | `model-domain` |
| `skill-rails-bug-triage` | `triage-bug` |
| `skill-ruby-service-objects` | `create-service-object` |

Rails-only scenarios stay in `igmarin/rails-agent-skills`.

```bash
bash scripts/validate-evals.sh
```
