{
  "target_skill": "implement-calculator-pattern",
  "source_path": "skills/patterns/implement-calculator-pattern/SKILL.md",
  "instructions": [
    {
      "instruction": "Write the spec/test — contexts per variant, plus the NullService path",
      "original_snippets": "Write the spec/test — contexts per variant, plus the NullService path",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run it — verify it fails because the component does not exist yet",
      "original_snippets": "Run it — verify it fails because the component does not exist yet",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run again — confirm green, then proceed to the next component",
      "original_snippets": "Run again — confirm green, then proceed to the next component",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "the next component starts. Do not collapse NullService and concrete services",
      "original_snippets": "the next component starts. Do not collapse NullService and concrete services",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "preference"
    },
    {
      "instruction": "Test coverage: Factory, NullService, and every concrete service must cover named variants, inactive plan, nil plan, and unknown variant contexts (or explicitly explain why a context does not apply).",
      "original_snippets": "Test coverage: Factory, NullService, and every concrete service must cover named variants, inactive plan, nil plan, and unknown variant contexts (or explicitly explain why a context does not apply).",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "preference"
    },
    {
      "instruction": "Create the **Factory**. No qualifying context or unknown variant → `NullService`.",
      "original_snippets": "Create the **Factory**. No qualifying context or unknown variant → `NullService`.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create the **BaseService**. Define `calculate` that delegates to `compute_result` if `should_calculate?` is true.",
      "original_snippets": "Create the **BaseService**. Define `calculate` that delegates to `compute_result` if `should_calculate?` is true.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create the **NullService**. Always return false for `should_calculate?` and nil for `compute_result`.",
      "original_snippets": "Create the **NullService**. Always return false for `should_calculate?` and nil for `compute_result`.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create **Concrete** services. Override `should_calculate?` and `compute_result`. Always call `super` in `should_calculate?`.",
      "original_snippets": "Create **Concrete** services. Override `should_calculate?` and `compute_result`. Always call `super` in `should_calculate?`.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run the full test suite.",
      "original_snippets": "Run the full test suite.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Verify the **Single entry point rule:** `Factory.for(entity)` is the **only** permitted access path.",
      "original_snippets": "Verify the **Single entry point rule:** `Factory.for(entity)` is the **only** permitted access path.",
      "relevant_when": "When a user asks for Ruby work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    }
  ]
}
